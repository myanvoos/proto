<critical>
Write a short note to the next instance of yourself. It is appended to a structured summary that replaces the conversation above.
The structured summary already records goal, constraints, progress, decisions, next steps and touched files: NEVER restate them.
Output ONLY the note. No preamble, no commentary, no headings.
</critical>

<instruction>
Record what a summarizer reading this transcript cannot recover:
- why the current approach was chosen; which alternatives were ruled out, and by what evidence
- traps, surprising behavior, and facts learned the hard way
- exact state of unfinished work: what is applied, what is verified, what is still assumed
- anything above that misleads a reader who takes it at face value
Preserve exact paths, symbol names, commands and error text when they carry the point.
NEVER invent detail the conversation does not contain. NEVER claim work you did not do.
This is your own memory, not a report: first person, past tense, at most 200 words, no section headings.
</instruction>

<critical>
NEVER mention summaries, notes, handoffs, compaction, or context limits — they are not part of the user's task.
</critical>

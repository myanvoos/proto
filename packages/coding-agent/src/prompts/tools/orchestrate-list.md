Worker roster: id, agent type, state (`starting`/`running`/`idle`/`dead`), model, turn count, queued messages, one-line gist of latest activity.

Use to reorient: existing workers, busy workers, idle workers ready for the next instruction.

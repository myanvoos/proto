Generates/edits images.

- One detailed `subject` prompt: generation or editing.
- Multiple `input` images: describe each one's role in `subject` (e.g. `Image 1` for composition, `Image 2` for lighting).
- Text in images: add "sharp, legible, correctly spelled"; keep short.

Describe the image so a model unable to see it can reason about its content.

Cover where present: scene, subject, action; people/objects — relationships, positions, colors, counts; all visible text verbatim; UI elements — labels, buttons, states, errors, disabled controls; diagrams/charts/tables — structure, axes, series, encoded values.

Flag anything ambiguous or unreadable. Plain prose only.

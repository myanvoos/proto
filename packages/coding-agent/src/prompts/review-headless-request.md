## Code Review Request

Mode: headless review request.

Distribution: Use `orchestrate_spawn` with `agent: "reviewer"`; create exactly **1 reviewer worker** for recent code changes.

{{#if focus}}
Focus: {{focus}}
{{/if}}

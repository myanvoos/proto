Epoch {{epoch}}.

Wake reasons:
{{wakeReasons}}

{{objective}}

Budget:
{{tokensUsed}}/{{tokenBudget}} tokens; {{remainingTokens}} remaining; {{timeUsedSeconds}} seconds.

<epoch-digest-source>
{{digest}}
</epoch-digest-source>

Digest: tempo evidence, not correctness; headline matters → MUST investigate with `read`/`bash`.

Call `cue` exactly once; NEVER narrate:
`cue({op:"next"})` on-track/default; `cue({op:"next", prompt})` digest-grounded redirect; `cue({op:"next", context:"compact"})` fold context; `cue({op:"next", note})` one-line rationale; `cue({op:"escalate", question})` ONLY user decision.
Every turn MUST read or exploratory `bash` before `cue`.

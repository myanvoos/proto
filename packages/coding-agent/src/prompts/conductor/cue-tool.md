Rule on the pended completion claim: exactly one call per verification turn.
`verdict:"accept"` only with direct current-state evidence for every deliverable; `verdict:"reject"` must enumerate concrete discrepancies.
`op:"escalate"` when the objective is unverifiable as written and only the user can resolve it.

Commission the contract for this stretch: exactly one call per commissioning turn.
`objective` MUST contain exactly the five ordered top-level sections (`## Objective`, `## Success criteria`, `## Verification`, `## Boundaries`, `## Stop conditions`) and no other headings. `## Verification` MUST contain at least one existing runnable command per nonempty line, enclosed in backticks.
`token_budget` only when the rough ask states one. `op:"amend"` and `op:"show"` are not available yet.

Commission the contract for this stretch: exactly one call per commissioning turn.
`objective` MUST be the five-section markdown (`## Objective`, `## Success criteria`, `## Verification`, `## Boundaries`, `## Stop conditions`) with machine-checkable criteria and verification commands that already exist in this repo.
`token_budget` only when the rough ask states one. `op:"amend"` and `op:"show"` are not available yet.

{
	"type": "module",
	"name": "@oh-my-pi/browser-relay",
	"version": "0.1.0",
	"private": true,
	"description": "Chrome extension that lets the proto browser tool drive your existing tabs (relay server lives in the proto CLI: `proto browser-relay`)",
	"homepage": "https://proto.sh",
	"author": "Stencil Labs, Inc.",
	"license": "MIT",
	"scripts": {
		"build": "bun scripts/build-extension.ts",
		"check": "biome check . && tsgo -p tsconfig.json --noEmit",
		"lint": "biome lint .",
		"fix": "biome check --write --unsafe .",
		"fmt": "biome format --write ."
	},
	"devDependencies": {
		"@types/bun": "catalog:"
	},
	"engines": {
		"bun": ">=1.3.14"
	}
}

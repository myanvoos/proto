/**
 * Parameter key used to carry intent tracing metadata on tool calls
 * (e.g. why a tool was invoked). Shared by the agent loop, inference
 * dialects, and transcript tooling.
 */
export const INTENT_FIELD = "i";
